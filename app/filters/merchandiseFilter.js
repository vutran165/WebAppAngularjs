ngFilters.filter("merchandiseFilter", [
    '$filter', function($filter) {
        return function(arrayList, arrayCondition) {
        }
        var resultData = [];

        angular.forEach(arrayList, function(item) {
            angular.forEach(arrayCondition, function(condition) {
                //if(item.)
            });
        });
    }
]);